// Optional: Prisma setup if needed in the future
// To use Prisma instead of Mongoose, install with:
// npm install @prisma/client
// npm install -D prisma

// Then run: npx prisma init
// And uncomment/use the prisma/schema.prisma below

/*
datasource db {
  provider = "mongodb"
  url      = env("MONGODB_URI")
}

generator client {
  provider = "prisma-client-js"
}

// Replace MONGO_URI with MONGODB_URI in your .env if using Prisma
// Example:
// MONGODB_URI="mongodb+srv://username:password@cluster.mongodb.net/feesportal"
*/
